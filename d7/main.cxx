#include <iostream>
#include <fstream>

using std::cout, std::endl;

int main() {
    std::ifstream file{"../d7/input.txt"};
    std::string line;

    while (std::getline(file, line)) {
        
    }
    
    return 0;
}
